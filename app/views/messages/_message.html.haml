.media
  = image_tag(model_image(message.user.avatar, 200, 200, true), width: 52, class: 'd-flex mr-3 rounded-circle')
  .media-body
    %p
      = message.text
    %small.text-muted
      = time_ago_in_words(message.created_at)
      ago
    %small
      
%hr
