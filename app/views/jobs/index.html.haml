.container.content_container  
  %h1
    = title "Jobs"
    %small
      = link_to 'Post A Job', new_job_path, class: "btn btn-info btn-sm" if policy(Job).new?
  %p
    %small
      = pluralize(Job.all.count, "job")

  = render @jobs

  = paginate @jobs
