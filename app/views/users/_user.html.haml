= cache [user] do
  .media
    = link_to image_tag(model_image(user.avatar, 200, 200, true), width: 52, class: 'd-flex mr-3 rounded-circle'), user
    .media-body
      %h5.mt-0
        = link_to user.name, user
      - if user.username.present?
        = "@#{user.username}"

  %hr
