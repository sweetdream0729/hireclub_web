.container.mt-5.mb-5
  

  %h1
    = @role.name
  %p
    %small
      = link_to 'Edit', edit_role_path(@role), class: "btn btn-primary btn-sm" if policy(@role).edit?
  %p
    %small
      = pluralize(@role.users_count, "member")

  = link_to 'Back', roles_path
