.container.mt-3.mb-5
  .row.justify-content-center
    .col-lg-6.col-md-8.col-sm-10
      .text-center.mt-3.mb-5
        %a{href: root_path}
          = image_tag("logo.svg", width: 30, height: 30, alt: "HireClub Logo")

      %h1 Unsubscribed

      %p.mt-4
        %strong
          = @user.email
        was unsubscribed from 
        = @mail_type
        mails

      / %p
      /   %a.btn.btn-lg.btn-info{href: settings_update_path}
      /     Resubscribe\
      %p
        - if @mail_type == "all"
          /= link_to "Resubscribe All", "#", class: 'btn btn-link'
        - else
          = link_to "Resubscribe", resubscribe_path(@user.preference_access_token(@preference, true)), class: 'btn btn-link'

      %hr
      %p
        %a.btn.btn-secondary{href: settings_notifications_path}
          Notification Settings
        - if @mail_type != "all"
          = link_to "Unsubscribe All", unsubscribe_path(@user.preference_access_token("unsubscribe_all", true)), class: 'btn btn-link'
      %p
        %small
          You will continue to receive essential account related information.