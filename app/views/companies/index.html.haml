= title "Companies"
.container.mt-5.mb-5
  %h1 
    Companies
    %small
      = link_to 'New', new_company_path, class: "btn btn-primary btn-sm" if policy(Company).new?
      = link_to 'Import', new_company_import_path, class: "btn btn-info btn-sm" if policy(Company).import?

  .companies.mt-4
    = render @companies

  = paginate @companies