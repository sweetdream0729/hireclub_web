= title @skill.name
.container.mt-5.mb-5
  / %a{:href => skills_path } « Skills

  %h1
    = @skill.name
  %p
    %small
      = pluralize(@skill.users_count, "member")


  = link_to 'Edit', edit_skill_path(@skill), class: "btn btn-primary" if policy(@skill).edit?
