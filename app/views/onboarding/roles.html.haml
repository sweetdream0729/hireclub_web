= title "What's your role?"

.container.mt-5.mb-5
  .row.justify-content-center
    .col-lg-6.col-md-8.col-sm-10
      %h1 What's your role?

      = form_for @user, url: wizard_path, method: :put do |f|
        = f.fields_for :user_roles do |user_role_form|
          .form-group
            = user_role_form.collection_select(:role_id, Role.by_name, :id, :name, {}, {class: "form-control"})
            %small.form-text.text-muted 
              You can add more roles later.

        = f.submit "Next", class: "btn btn-primary btn-lg"

